# MUST-READ — Dated source: 2026-05-31

This is the dated source of truth for `MUST-READ.md` at the project root. When the root pointer is older than this file, refresh it.

See the root `MUST-READ.md` for the full text. This dated file exists so future sessions can audit what was true on this exact day.

Today's session was an extremely long Opus + 7-Sonnet-dispatch sweep. Net result:

- **17 production commits** shipped (`156e03f` → `172489f` + several follow-ups)
- **9 "stubborn" bugs** root-caused and fixed permanently (see root MUST-READ §2)
- **Demucs + Basic Pitch installed** on the Contabo server (no GPU, but CPU-mode works)
- **4 Sonnet sub-agents** dispatched in parallel for: music genre picker, words-on-image, vocal-cleanup route, melody-extract route — all returned clean
- **3 Sonnet sub-agents** dispatched in parallel for: licence manifest audit, Free Mode beats-first picker, music download script
- **Safe music policy** locked: no Incompetech, no attribution-required tracks

Most painful lesson: **divergent narration-firing paths** (3 different ones in children-planner) caused the same BIB symptom three times in a row. Audit ALL TTS callers when fixing one.

For everything else, see the root MUST-READ.md.
